services:
  webapp:
    build:
      target: production
    restart: always
    environment:
      - FLASK_ENV=production
    networks:
      backend:
      nginx:
          aliases:
          - gavmild-webapp

networks:
  nginx:
    external: true
    name: www_nginx